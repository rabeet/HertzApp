<div class="media">
  <div class="media-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>IC #</th>
          <th colspan="3"></th>
        </tr>
      </thead>
    
      <tbody>
        <% all_products.each do |a| %>
          <tr>
            <% if a.ic %>
              <td><%= a.ic %></td>
            <% else %>
              <td>Product <%= a.id %></td>
            <% end %>
            <td>
              <%= link_to 'View Transactions', '#', class: 'btn btn-xs btn-default' %>
              <%= link_to "Edit", edit_product_path( product ) %> | 
              <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>