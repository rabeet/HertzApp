{
  num_pruducts=<%= @num_products %>,
  products=[
  <% i = 0 %>
  <% @products.each do |product| %>
    <%= i == 0 ? "" : ", " %>
    {
      id=<%= product.id %>,
      ic_num=<%= product.ic %>,
      name="<%= product.name %>"
    }
  <% end %>
  ]
}